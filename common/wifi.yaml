wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  manual_ip:
    gateway: !secret ip_gateway
    subnet: !secret ip_netmask
    dns1: !secret ip_dns_primary
    dns2: !secret ip_dns_secondary
